-- Danger: extensions are shared; do not drop in shared environments.
-- For local rollback only, you may drop if needed.
-- drop extension if exists "uuid-ossp";
-- drop extension if exists pgcrypto;


